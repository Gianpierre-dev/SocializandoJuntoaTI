---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/base-layout.astro";
import { site } from "../data/site";
import SectionHeading from "../components/ui/section-heading.astro";
import TeamGrid from "../components/sections/team-grid.astro";
import AlliesStrip from "../components/sections/allies-strip.astro";

const values = (await getCollection("values")).sort(
  (a, b) => a.data.order - b.data.order,
);

const pageTitle = `Nosotros | ${site.name}`;
const pageDescription =
  "Conoce nuestra misión, visión y los valores que guían a Socializando Junto A Ti, una organización juvenil dedicada al bienestar de la salud mental de adolescentes y jóvenes.";
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <!-- Intro -->
  <section class="bg-surface py-16 sm:py-20">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <SectionHeading
        title="Nosotros"
        subtitle={`${site.name} es una organización juvenil fundada el ${site.foundedDate}, dedicada a ${site.audience.toLowerCase()}.`}
      />
    </div>
  </section>

  <!-- Mission and Vision -->
  <section class="bg-subtle py-16 sm:py-20">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <div class="grid gap-8 md:grid-cols-2">
        <article class="rounded-2xl border border-line bg-surface p-8 shadow-sm">
          <h2 class="text-2xl font-semibold text-accent">Misión</h2>
          <p class="mt-4 text-base leading-relaxed text-content/80">
            {site.mission}
          </p>
        </article>
        <article class="rounded-2xl border border-gold/20 bg-surface p-8 shadow-sm">
          <h2 class="text-2xl font-semibold text-gold">Visión</h2>
          <p class="mt-4 text-base leading-relaxed text-content/80">
            {site.vision}
          </p>
        </article>
      </div>

      <article class="mt-8 rounded-2xl border border-line bg-surface p-8 shadow-sm">
        <h2 class="text-2xl font-semibold text-content">Objetivo</h2>
        <p class="mt-4 text-base leading-relaxed text-content/80">
          {site.objective}
        </p>
      </article>
    </div>
  </section>

  <!-- Values -->
  <section class="bg-surface py-16 sm:py-20">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <SectionHeading
        title="Nuestros valores"
        subtitle="Los principios que guían cada actividad y cada encuentro."
      />

      {
        values.length > 0 ? (
          <ul class="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
            {values.map((value) => (
              <li class="rounded-xl border border-line bg-subtle p-6 shadow-sm">
                <h3 class="font-semibold text-content">{value.data.title}</h3>
                <p class="mt-2 text-sm leading-relaxed text-content/70">
                  {value.data.description}
                </p>
              </li>
            ))}
          </ul>
        ) : (
          <p class="mt-12 text-center text-content/60">
            Pronto compartiremos nuestros valores.
          </p>
        )
      }
    </div>
  </section>

  <!-- Team -->
  <TeamGrid />

  <!-- Allies -->
  <AlliesStrip />
</BaseLayout>
