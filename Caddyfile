# Railway injects PORT; default to 80 for local runs.
:{$PORT:80} {
	root * /srv
	encode gzip zstd

	# Astro builds directory-style routes (/nosotros/index.html).
	# Resolve clean URLs to their index file.
	try_files {path} {path}/index.html

	file_server
}
